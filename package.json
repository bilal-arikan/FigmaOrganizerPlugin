{
  "name": "figma-unity-bridge-organizer",
  "version": "1.0.0",
  "description": "Figma plugin that applies JSON rules prepared to Figma documents",
  "main": "code.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "npm run build && npm run watch",
    "analyze": "npx ts-node analyzer/index.ts",
    "server": "npm run build && node dist/analyzer/server.js"
  },
  "keywords": [
    "figma",
    "plugin",
    "unity",
    "components",
    "automation"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.123.0",
    "@types/node": "^25.2.3",
    "typescript": "^4.9.0"
  }
}
